'use client';

export function VehicleFilters({
  resultCount,
}: {
  facets: unknown;
  resultCount: number;
  basePath?: string;
}) {
  return (
    <section aria-label="Vehicle results" className="space-y-4">
    </section>
  );
}
